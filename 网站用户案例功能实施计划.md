# 网站用户案例功能实施计划

## 📋 需求总结
为每个提交的网站添加用户案例集功能：
- 每个案例包含：标题、描述内容、**外部链接**（新增）、可选封面图
- 在网站卡片上以"折叠展开"方式显示案例
- 支持添加多个案例

---

## 🔧 实施步骤

### 1. 数据库迁移
**文件：** `prisma/schema.prisma`

**操作：**
- 在 `UseCase` 模型中添加 `external_link` 字段
- 字段类型：`String?`（可选）
- 用途：存储案例相关的外部链接（演示链接、文档链接等）

**执行命令：**
```bash
npx prisma migrate dev --name add_external_link_to_use_cases
npx prisma generate
```

---

### 2. 更新 TypeScript 类型
**文件：** `src/lib/types.ts`

**操作：**
- 在 `UseCase` 接口添加：
  ```typescript
  external_link?: string;
  ```
- 在 `UseCaseFormInputs` 接口添加：
  ```typescript
  external_link?: string;
  ```

---

### 3. 更新验证 Schema
**文件：** `src/lib/utils/validations.ts`

**操作：**
- 在 `useCaseFormSchema` 中添加：
  ```typescript
  external_link: z.string().url("请输入有效的网址").optional(),
  ```

---

### 4. 更新 API
**文件：** `src/app/api/use-cases/route.ts`

**操作：**
- 修改 `POST` 方法，支持接收并保存 `external_link` 字段
- 创建案例时包含 `external_link: data.external_link || null`

---

### 5. 创建案例展示组件
**新建文件：** `src/components/website/website-use-cases.tsx`

**功能设计：**
- 显示网站关联的所有已发布案例
- UI：折叠/展开设计
  - 折叠状态：显示 "查看 X 个案例" 按钮
  - 展开状态：案例列表，每个案例显示：
    - 案例标题
    - 预览描述（截取前100字符）
    - 外部链接按钮（如果有）
- 使用 framer-motion 实现展开/收起动画

**Props：**
```typescript
interface WebsiteUseCasesProps {
  websiteId: number;
}
```

---

### 6. 修改网站卡片
**文件：** `src/components/website/website-card.tsx`

**操作：**
- 导入 `WebsiteUseCases` 组件
- 在卡片底部（点赞/踩按钮下方）添加案例区域
- 添加状态管理：
  - `showUseCases`: 控制案例区域展开/收起
- 集成 `WebsiteUseCases` 组件

---

### 7. 更新案例表单
**文件：** `src/components/forms/use-case-form.tsx`

**操作：**
- 在 "案例内容" 之后添加 "外部链接" 输入框
- 使用 `FormField` 组件
- 占位符：`https://example.com/demo`
- 提示文字："案例相关的外部链接（可选）"

---

### 8. 创建案例数据 API（可选优化）
**新建文件：** `src/app/api/websites/[id]/use-cases/route.ts`

**功能：**
- GET 方法：获取指定网站的所有已发布案例
- 按创建时间倒序排列
- 返回格式：
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "案例标题",
        "content": "案例内容",
        "external_link": "https://...",
        "created_at": "2026-01-31"
      }
    ]
  }
  ```

---

## 📁 文件变更清单

| 操作 | 文件路径 | 说明 |
|------|---------|------|
| 修改 | `prisma/schema.prisma` | 添加 external_link 字段到 UseCase 模型 |
| 修改 | `src/lib/types.ts` | 更新 UseCase 和 UseCaseFormInputs 接口 |
| 修改 | `src/lib/utils/validations.ts` | 添加 external_link URL 验证 |
| 修改 | `src/app/api/use-cases/route.ts` | 支持保存 external_link 字段 |
| **新建** | `src/components/website/website-use-cases.tsx` | 案例展示组件（折叠展开） |
| 修改 | `src/components/website/website-card.tsx` | 集成案例显示功能 |
| 修改 | `src/components/forms/use-case-form.tsx` | 添加外部链接输入框 |
| **新建** | `src/app/api/websites/[id]/use-cases/route.ts` | 获取网站案例的API |

---

## ✅ 验证步骤

### 1. 类型检查
```bash
npx tsc --noEmit
```

### 2. 代码规范检查
```bash
npm run lint
```

### 3. 数据库迁移
```bash
npx prisma migrate dev --name add_external_link_to_use_cases
npx prisma generate
```

### 4. 功能测试
- 启动开发服务器：`npm run dev`
- 测试案例添加（包含外部链接）
- 测试网站卡片案例显示
- 测试案例折叠/展开交互

### 5. 构建验证
```bash
npm run build
```

---

## 📊 UI 设计预览

### 网站卡片 - 折叠状态
```
┌─────────────────────────────┐
│  [ 缩略图区域 ]             │
├─────────────────────────────┤
│  网站标题                   │
│  网站描述                   │
│  👍 10  👎 2               │
│  [查看 3 个案例 ▼]         │
└─────────────────────────────┘
```

### 网站卡片 - 展开状态
```
┌─────────────────────────────┐
│  [ 缩略图区域 ]             │
├─────────────────────────────┤
│  网站标题                   │
│  网站描述                   │
│  👍 10  👎 2               │
│  [收起案例 ▲]              │
│  ┌───────────────────────┐ │
│  │ 案例1: 标题           │ │
│  │       预览描述...    │ │
│  │       [查看演示 →]   │ │
│  ├───────────────────────┤ │
│  │ 案例2: 标题           │ │
│  │       预览描述...    │ │
│  ├───────────────────────┤ │
│  │ 案例3: 标题           │ │
│  │       预览描述...    │ │
│  └───────────────────────┘ │
└─────────────────────────────┘
```

---

## 📝 备注

1. **兼容性**：新增字段为可选，不影响现有数据
2. **性能**：使用客户端组件，案例数据按需加载
3. **可扩展**：未来可添加案例编辑、删除功能
4. **离线部署**：所有功能均使用本地资源，符合离线部署要求

---

**计划制定日期：** 2026-01-31
**项目路径：** `/Users/chenmingmin/Documents/GitHub/ai-navigation`
